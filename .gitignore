# Compiled object files
*.o
*.obj

# Compiled binaries / executables
*.exe
*.out
*.a
*.lib
*.dll
*.so
*.dylib
deaddrop
!*.c
!*.h

# Debug/build directories
build/
build_san/
bin/
obj/

# Editor / OS cruft
.vscode/
.vs/
*.swp
*~
.DS_Store
Thumbs.db

# Valgrind / sanitizer output
*.log

# Piper TTS voice models - large binary assets, device-local only, never
# committed. See include/hw_tts.h (HW_TTS_MODEL_PATH) for where the
# actual deployed copy lives on each Pi.
*.onnx
*.onnx.json

# Local-only comment archive - the full narrative context (bug-fix
# history, design rationale) trimmed out of source comments. Kept for
# local reference only; never committed or pushed, so the public GitHub
# copy shows only clean, minimal source comments.
docs/COMMENT_ARCHIVE.md